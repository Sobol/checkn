<%= form_for @device_photo, :url => admin_device_photos_path, :html => { :multipart => true } do |f| %>
  <%= render 'admin/shared/error_messages', :object => f.object -%>
  <table>
    <tr>
      <td><%= f.label :name, 'Nazwa:' %></td>
    </tr>
    <tr>
      <td><%= f.text_field :name %></td>
    </tr>
    <tr>
      <td><%= f.label :description, 'Opis:' %></td>
    </tr>
    <tr>
      <td><%= f.text_field :description %></td>
    </tr>
    <tr>
      <td>Dodaj zdjęcie:</td>
    </tr>
    <tr>
      <td>  
        <%= f.file_field :photo %>
      </td>
    </tr>
    <tr>
      <td>
        <%= f.submit "Zapisz", :class => "left" %>
        <%= link_to "Cofnij", admin_device_photos_path, :class => "left" %>
      </td>
    </tr>
  </table>
<% end %>
